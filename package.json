{
  "name": "aibou",
  "version": "1.0.0",
  "private": true,
  "description": "Remote control for locally running Kiro agent sessions",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm --filter @aibou/protocol build && pnpm --filter @aibou/pwa build && pnpm --filter @aibou/bridge build && pnpm --filter @aibou/mock-agent build",
    "dev": "pnpm --filter @aibou/bridge dev",
    "demo": "pnpm --filter @aibou/protocol build && pnpm --filter @aibou/mock-agent build && pnpm --filter @aibou/pwa build && pnpm --filter @aibou/bridge demo",
    "start": "pnpm run build && pnpm --filter @aibou/bridge start",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "check": "pnpm run typecheck && pnpm run lint && pnpm run test",
    "chat": "node packages/bridge/dist/chat/cli.js",
    "approve": "node scripts/trigger-approval.mjs \"Run the shell command 'node --version' and tell me what it prints.\"",
    "watch:live": "node scripts/watch-live.mjs",
    "verify:watch": "node scripts/verify-watch-live.mjs",
    "doctor": "node scripts/doctor.mjs",
    "verify": "node scripts/verify-all.mjs",
    "verify:quick": "node scripts/verify-all.mjs --quick",
    "verify:node": "node scripts/verify-all.mjs --skip-device"
  },
  "keywords": [
    "kiro",
    "acp",
    "agent",
    "remote",
    "approval",
    "wear-os"
  ],
  "license": "MIT",
  "devDependencies": {
    "ws": "^8.21.3"
  }
}
